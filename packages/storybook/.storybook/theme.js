import { create } from '@storybook/theming';
import logo from '../assets/barand-logo.png';
export default create({
  base: 'light',
  brandUrl: '/',
  brandTitle: 'To The Moon',
  brandImage: logo,
});
